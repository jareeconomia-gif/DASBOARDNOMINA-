{
  "name": "subtec-nomina-dashboard",
  "version": "2.0.1",
  "private": true,
  "description": "Dashboard histórico de nómina SUBTEC con base compartida",
  "main": "server.js",
  "scripts": {
    "start": "node patch-index.js && node server.js",
    "check": "node --check patch-index.js && node --check server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "pg": "^8.16.3"
  }
}
